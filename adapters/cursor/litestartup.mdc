---
description: LiteStartup skills suite — publish content, send emails, and more
globs: ["blog/**", "docs/**", "website/**", "changelog/**", "litestartup.yaml"]
---

# LiteStartup Skills

Modular AI skill packages for LiteStartup.

## Skill Router

| Skill | Intent | Entry Point |
|-------|--------|-------------|
| litestartup-publish | publish, sync, write blog/docs/website/changelog, send email, bind repo | `skills/litestartup-publish/SKILL.md` |
| litestartup-admin | init saas, create project, scaffold, configure, deploy | `skills/litestartup-admin/SKILL.md` |

## File References

- Publish Skill: `skills/litestartup-publish/SKILL.md`
  - References: `skills/litestartup-publish/references/{bind,sync,email,status,docs,blog,website,changelog,campaign}.md`
  - Assets: `skills/litestartup-publish/assets/`
  - Scripts: `skills/litestartup-publish/scripts/` (Linux/macOS fallback)
- Admin Skill: `skills/litestartup-admin/SKILL.md`
  - References: `skills/litestartup-admin/references/{init,configure,status,env-config,ls-capabilities,features}.md`
  - Assets: `skills/litestartup-admin/assets/`

## Rules

1. NEVER display API keys (`~/.litestartup/credentials`)
2. **Skill Isolation**: admin and publish are INDEPENDENT skills with different prerequisites:
   - admin (init/configure) → checks `.env.local`, does NOT need `litestartup.yaml` or git remote
   - publish (bind/sync) → checks `litestartup.yaml`, needs git remote
3. For publish: look for `litestartup.yaml` to find the content repo
4. Read the relevant spec BEFORE writing any content
5. For website pages: confirm type (website vs block) with user first
6. After writing: sync per `skills/litestartup-publish/references/sync.md` (Windows → AI-native / Linux → script)
